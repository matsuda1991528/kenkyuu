#include<stdio.h>#include<stdlib.h>#include"header.h"/* 実行時コマンドのエラーチェック */void checkIncludeCommand(int start_vertex, int goal_vertex){	if(start_vertex == 0 || goal_vertex == 0){		printf("Do you forgot input include command?\n");		exit(1);	}	else{		return;	}}void printVertexList(int num, struct adj_list_t* adj_list_head, struct adj_list_t* adj_list_p){	adj_list_p = adj_list_head;	printf("start = %d\n", num);	while(adj_list_p != NULL){		printf("num = %d\n", adj_list_p->num);		adj_list_p = adj_list_p->next;	}	adj_list_p = adj_list_head;	return;}		