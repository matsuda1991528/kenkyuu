#include<stdio.h>#include<stdlib.h>#include"header.h"/* 実行時コマンドの初期化それぞれの変数に空を示すEMPTYを代入 */void initIncludeCommand(struct include_command_data_t* input){	input->start_vertex = EMPTY;	input->goal_vertex = EMPTY;	input->dptr.hour = EMPTY;	input->dptr.min = EMPTY;	input->vel_kph = EMPTY;	return;}/* 実行時コマンドのエラーチェック */void checkIncludeCommand(struct include_command_data_t input){	if(input.start_vertex == EMPTY){		printf("Do you forgot input include command?\n");		exit(1);	}	else if(input.goal_vertex == EMPTY){		printf("Do you forgot input include command?\n");		exit(1);	}	else if(input.dptr.hour == EMPTY){		printf("Do you forgot input include command?\n");		exit(1);	}	else if(input.dptr.min == EMPTY){		printf("Do you forgot input include command?\n");		exit(1);	}	else if(input.vel_kph == EMPTY){		printf("Do you forgot input include command?\n");		exit(1);	}	else{		return;	}}/* 入力したスタート位置，ゴール位置が地図上に存在するか否かのチェック 存在しない場合はエラーメッセージをだし，強制終了*/void checkStartAndGoalNum(int start, int goal, int node_size){	if(start <= 0 || start >= node_size){		printf("%d is not expected start pos!!!\n", start);		exit(1);	}	else if(goal <= 0 || goal >= node_size){		printf("%d is not expected goal pos\n", goal);		exit(1);	}	else{		return;	}}void printVertexList(int num, struct adj_list_t* adj_list_head, struct adj_list_t* adj_list_p){	adj_list_p = adj_list_head;	printf("start = %d\n", num);	while(adj_list_p != NULL){		printf("num = %d\n", adj_list_p->num);		adj_list_p = adj_list_p->next;	}	adj_list_p = adj_list_head;	return;}		